export const InputLabel = ({ label }: { label: string }) => {
  return (
    <label className="block uppercase tracking-wide text-left ml-1 text-slate-700 text-xs sm:text-sm font-bold mb-1">
      {label}
    </label>
  );
};
